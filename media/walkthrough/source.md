# View or Edit the Source

Run **DITA Editor: View/Edit Source** to reopen the same file in VS Code's text
editor. You can switch between the visual and source views freely.

Because every visual edit is a minimal diff, the DITA XML round-trips
byte-for-byte — nothing is reformatted behind your back.

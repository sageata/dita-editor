# Open the Visual Editor

Open a `.dita` topic, then choose **DITA Editor: Open Visual Editor** — from the
editor toolbar (the preview icon) or the Command Palette.

The document opens in a WYSIWYG canvas. Your file is never reformatted: visual
edits are written back to disk as minimal diffs.
